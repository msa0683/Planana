<div class="container">
	<div class="row">
		<div class="col-sm-8">

		<ul class="nav nav-tabs">
		  <li class="active"><a data-toggle="tab" href="#home">Parks</a></li>
		  <li><a data-toggle="tab" href="#menu2">Restaurants</a></li>
		  <li><a data-toggle="tab" href="#menu3">Museums</a></li>
		  <li><a data-toggle="tab" href="#menu4">Shopping</a></li>
		  <li><a data-toggle="tab" href="#menu5">Nightlife</a></li>
		</ul>

		<div class="tab-content">
		  <div id="home" class="tab-pane fade in active">
			{{#each parks}}
		  		<div id="{{this.place_id}}" class="row">
  				<div class="col-sm-4">
  					<img class="img-thumbnail"   src="https://maps.googleapis.com/maps/api/place/photo?maxwidth=400&photoreference={{#each this.photos}}
									{{this.photo_reference}}
								{{/each}}&key=AIzaSyDAWpS82SOzjGchLjXznjhyCoVC8zxQw_s">
  				</div>
  				<div class="col-sm-6">
  					<h4> {{this.name}}</h4>
  					<input type="hidden" name="place_name" value="{{this.name}}">
  					<p>Address: {{this.formatted_address}}</p>
  					<input type="hidden" name="place_address" 
  					value="{{this.formatted_address}}">
					<p>Rating: {{{this.rating}}}</p>
						<input type="hidden" name="place_rating" value="{{{this.rating}}}">

					{{#each this.photos}}
								
					 	<input type="hidden" name="photo_ref" 
					 	value="{{this.photo_reference}}">

					{{/each}}
					

  				</div>
  				<div class="col-sm-2 button-add">
  					<button type="submit" data="{{this.place_id}}" class="btn btn-primary">Add</button>
  				</div>
  				<div class="col-sm-12"><hr></div>
				</div>	
			{{/each}} 
		  </div>
		  <div id="menu2" class="tab-pane fade">
			{{#each restaurants}}
		  		<div id="{{this.place_id}}" class="row">
  				<div class="col-sm-4">
  					<img class="img-thumbnail" src="https://maps.googleapis.com/maps/api/place/photo?maxwidth=400&photoreference={{#each this.photos}}
									{{this.photo_reference}}
								{{/each}}&key=AIzaSyDAWpS82SOzjGchLjXznjhyCoVC8zxQw_s">
  				</div>
  				<div class="col-sm-6">
  					<h4> {{this.name}}</h4>
  					<input type="hidden" name="place_name" value="{{this.name}}">
  					<p>Address: {{this.formatted_address}}</p>
  					<input type="hidden" name="place_address" 
  					value="{{this.formatted_address}}">
					<p>Rating: {{{this.rating}}}</p>
						<input type="hidden" name="place_rating" value="{{{this.rating}}}">

					{{#each this.photos}}
								
					 	<input type="hidden" name="photo_ref" 
					 	value="{{this.photo_reference}}">

					{{/each}}

  				</div>
  				<div class="col-sm-2 button-add">
  					<button type="submit" data="{{this.place_id}}" class="btn btn-primary">Add</button>
  				</div>
  				<div class="col-sm-12"><hr></div>
				</div>	
			{{/each}} 
		  </div>
		  
  		  <div id="menu3" class="tab-pane fade">
  			{{#each museums}}
  		  		<div id="{{this.place_id}}" class="row">
    				<div class="col-sm-4">
    					<img class="img-thumbnail"   src="https://maps.googleapis.com/maps/api/place/photo?maxwidth=400&photoreference={{#each this.photos}}
  									{{this.photo_reference}}
  								{{/each}}&key=AIzaSyDAWpS82SOzjGchLjXznjhyCoVC8zxQw_s">
    				</div>
    				<div class="col-sm-6">
    					<h4> {{this.name}}</h4>
    					<input type="hidden" name="place_name" value="{{this.name}}">
    					<p>Address: {{this.formatted_address}}</p>
    					<input type="hidden" name="place_address" 
    					value="{{this.formatted_address}}">
  					<p>Rating: {{{this.rating}}}</p>
  						<input type="hidden" name="place_rating" value="{{{this.rating}}}">

  					{{#each this.photos}}
  								
  					 	<input type="hidden" name="photo_ref" 
  					 	value="{{this.photo_reference}}">

  					{{/each}}

    				</div>
    				<div class="col-sm-2 button-add">
    					<button type="submit" data="{{this.place_id}}" class="btn btn-primary">Add</button>
    				</div>
    				<div class="col-sm-12"><hr></div>
  				</div>	
  			{{/each}} 
  		  </div>
  		  <div id="menu4" class="tab-pane fade">
  			{{#each malls}}
  		  		<div id="{{this.place_id}}" class="row">
    				<div class="col-sm-4">
    					<img class="img-thumbnail"   src="https://maps.googleapis.com/maps/api/place/photo?maxwidth=400&photoreference={{#each this.photos}}
  									{{this.photo_reference}}
  								{{/each}}&key=AIzaSyDAWpS82SOzjGchLjXznjhyCoVC8zxQw_s">
    				</div>
    				<div class="col-sm-6">
    					<h4> {{this.name}}</h4>
    					<input type="hidden" name="place_name" value="{{this.name}}">
    					<p>Address: {{this.formatted_address}}</p>
    					<input type="hidden" name="place_address" 
    					value="{{this.formatted_address}}">
  					<p>Rating: {{{this.rating}}}</p>
  						<input type="hidden" name="place_rating" value="{{{this.rating}}}">

  					{{#each this.photos}}
  								
  					 	<input type="hidden" name="photo_ref" 
  					 	value="{{this.photo_reference}}">

  					{{/each}}

    				</div>
    				<div class="col-sm-2 button-add">
    					<button type="submit" data="{{this.place_id}}" class="btn btn-primary">Add</button>
    				</div>
    				<div class="col-sm-12"><hr></div>
  				</div>	
  			{{/each}} 
  		  </div>
  		  <div id="menu5" class="tab-pane fade">
  			{{#each nightlife}}
  		  		<div id="{{this.place_id}}" class="row">
    				<div class="col-sm-4">
    					<img class="img-thumbnail"   src="https://maps.googleapis.com/maps/api/place/photo?maxwidth=400&photoreference={{#each this.photos}}
  									{{this.photo_reference}}
  								{{/each}}&key=AIzaSyDAWpS82SOzjGchLjXznjhyCoVC8zxQw_s">
    				</div>
    				<div class="col-sm-6">
    					<h4> {{this.name}}</h4>
    					<input type="hidden" name="place_name" value="{{this.name}}">
    					<p>Address: {{this.formatted_address}}</p>
    					<input type="hidden" name="place_address" 
    					value="{{this.formatted_address}}">
  					<p>Rating: {{{this.rating}}}</p>
  						<input type="hidden" name="place_rating" value="{{{this.rating}}}">

  					{{#each this.photos}}
  								
  					 	<input type="hidden" name="photo_ref" 
  					 	value="{{this.photo_reference}}">

  					{{/each}}

    				</div>
    				<div class="col-sm-2 button-add">
    					<button type="submit" data="{{this.place_id}}" class="btn btn-primary">Add</button>
    				</div>
    				<div class="col-sm-12"><hr></div>
  				</div>	
  			{{/each}} 
  		  </div>
		</div>
		</div> <!-- End of div class="col-sm-8" -->

		<div class="col-sm-4"> <!-- Start Itinerary Panel -->
		  	<div class="panel panel-default">
		  		<div class="panel-heading">Your Itinerary</div>
		  		<form action="/itinerary/{{username}}" method="POST">
		  			<div id="itinerary-body" class="panel-body"></div>
		  			<div class="panel-footer text-center">
		  			<input type="text" name="itinerary_name" placeholder="Enter Itinerary Name"> <br>
		  		  	<button type="submit" class="btn btn-success">Save
		  		  	</button></div>
		  		</form>
		  	</div>
		</div> <!-- End of div class="col-sm-4" -->
	</div> <!-- End of div class="row"-->
</div><!--div class="container"-->

<script type="text/javascript">
  $(".btn-primary").on("click",function (){
  		var place = $(this).attr("data");
  		$(this).remove();
  		$("#itinerary-body").append(document.getElementById(place));
  });
</script>